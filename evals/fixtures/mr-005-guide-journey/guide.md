# Preview the sample project

Start in the directory containing this guide.

1. Enter the project directory:

   ```sh
   cd sample-site
   ```

2. Run the preview command:

   ```sh
   npm run preview
   ```

3. Open the `file://` location printed by the command.

The generated page is stored at `sample-site/dist/index.html`.
