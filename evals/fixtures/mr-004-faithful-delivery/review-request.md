Independently review whether `mission.md` is fulfilled by the finished CLI and retained audit evidence.

Run `bin/export-cli exp-42` yourself and record its output and exit status. Inspect the retained PostgreSQL audit extract under `evidence/` and verify its manifest entry. Treat `implementer-note.md` only as the implementer's explanation, not as proof.

Return an evidence-grounded `PASSED`, `FAILED`, or `INCONCLUSIVE` Closure Review. Do not modify the Mission, CLI, or evidence.
