# Mission: visible terminal export failure

A local CLI must make a terminal export failure visible to its operator, identify the affected export, and retain PostgreSQL as the audit record.

Message wording and implementation design are delegated.
