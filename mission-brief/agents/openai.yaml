interface:
  display_name: "Mission Brief"
  short_description: "Freeze one traceable result contract for a fresh agent"
  default_prompt: "Use $mission-brief to freeze this adopted commission into a traceable result contract."
policy:
  allow_implicit_invocation: false
