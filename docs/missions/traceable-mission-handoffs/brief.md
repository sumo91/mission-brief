# Mission Brief: Traceable Mission handoffs preserve contract and material context

## Outcome

`mission-brief` continues to produce a compact, stable result contract while ensuring that material source information remains durably recoverable with its actual authority status. Mission 0 and child Missions use one predictable storage model that expresses result topology without turning folders into implementation phases or ownership fiction.

## Success

- A request for a final plan, design review, decision recommendation, or implementation plan is not silently replaced by a Mission Brief.
- Adopted decisions and applicable authority retain their necessary meaning, while retaining or linking mixed source material does not promote candidate approaches, critiques, or recommendations into binding content.
- Material known dependencies, concrete risk mechanisms, compatibility facts, and costly completed investigation receive a durable disposition instead of disappearing as implementation detail; safely discoverable unknowns remain delegated.
- A temporary path, attachment, or source conversation is not treated as a durable handoff when material information exists only there.
- A standalone Mission defaults to `docs/missions/<outcome-slug>/brief.md`. Mission 0 uses the same `brief.md` artifact with a visible parent role and `Result Boundaries`; commissioned children are nested only within one documentation boundary, while cross-boundary topology uses explicit links.
- Small settled Missions remain compact and do not acquire mandatory context records, source inventories, child directories, implementation plans, or review ceremony.
- A fresh Agent can recover the contract, locate material context, distinguish Authority Sources from Reference Sources, identify optional routes, and propose a viable implementation without the authoring conversation.

## Evidence Required

- A retrospective run against baseline revision `8adf782bf61e7051f9afe14d2e25166790e8bdc3` preserves its exact candidate identity and records whether the target compression defect reproduces on the strongest available regression input. The authentic feedback remains direct evidence that the defect occurred; unavailable historical inputs keep exact reproduction explicitly `INCONCLUSIVE` rather than being reconstructed as originals.
- Every active Mission Brief scenario and variant runs against one frozen candidate. Prompts, source fixtures, candidate hashes, raw turns, outputs, workspace diffs, grading, and an aggregate evidence manifest remain under `evals/runs/` rather than `/tmp`.
- The suite directly exercises final-plan fidelity, mixed authority, temporary sources, known findings versus safe discovery, irrelevant bulk, small Missions, standalone storage, same-boundary parent and child topology, cross-package topology, bidirectional links, and update-versus-new-result behavior.
- Separate contract-only and traceability-aware blind handoffs recover the commission without the authoring conversation. The traceability-aware reader distinguishes authority from reference and proposes a viable route that is not merely a paraphrase of a preserved candidate route.
- Static validation covers both runtime Skill bundles, UI metadata, fixture hashes, maintained local links, migrated paths, and the minimal runtime-file boundary.
- An independent Mission Review receives the frozen candidate and only durable evidence, treats prior verdicts as claims, and returns an honest `PASSED`, `FAILED`, or `INCONCLUSIVE`. Runtime synchronization occurs only after an evidence-supported `PASSED`.

## Boundaries

- Preserve explicit-only invocation, outcome-centered commissioning, authority-conflict handling, proportionate evidence, Mission 0 seam semantics, and implementation-route freedom.
- Do not turn Mission Brief into a complete plan, knowledge archive, sentence-by-sentence source map, fixed-length template, or mandatory Mission Package folder set.
- Keep maintainer context, ADRs, evals, fixtures, runs, and reviews outside the runtime Skill bundles.
- Preserve frozen historical fixtures as captured evidence; migrate live documentation references without rewriting historical inputs merely to match the new convention.
- Preserve source authenticity: a reconstruction, summary, or synthetic regression cannot be labeled as a missing original artifact.
- Do not broadly redesign the independent Mission Review behavior beyond the source-authority and co-located-review alignment required by this Mission.

## Context

- **Authority Source:** [`working-plan.md`](./working-plan.md) durably preserves the user-adopted implementation plan.
- **Reference Source:** [`context.md`](./context.md) records source status, available originals, missing historical materials, and review evidence.
- **Reference Source:** [`CONTEXT.md`](../../../CONTEXT.md) defines the domain language.
- **Authority Source:** [`docs/adr/0001-separate-mission-contract-from-context.md`](../../adr/0001-separate-mission-contract-from-context.md) records the adopted contract–context boundary.
- **Reference Source:** [`EVALS.md`](../../../EVALS.md) defines maintainer behavior checks and the detailed-plan preservation fixture.
- Baseline revision before this Mission: `8adf782bf61e7051f9afe14d2e25166790e8bdc3`.
